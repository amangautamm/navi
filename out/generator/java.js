"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSpringBootPomXml = getSpringBootPomXml;
exports.getSpringBootApplicationJava = getSpringBootApplicationJava;
exports.getSpringBootAppProperties = getSpringBootAppProperties;
exports.getSpringBootHealthController = getSpringBootHealthController;
exports.getSpringBootDockerfile = getSpringBootDockerfile;
exports.getJavaGitignore = getJavaGitignore;
const groupId = 'com.navi';
function getArtifactId(config) {
    return config.projectName.toLowerCase().replace(/\s+/g, '-');
}
function getPackageName(config) {
    return `${groupId}.${config.projectName.toLowerCase().replace(/[\s-]/g, '')}`;
}
// ─── pom.xml ──────────────────────────────────────────────────────────────────
function getSpringBootPomXml(config) {
    const artifactId = getArtifactId(config);
    const hasAuth = config.features.includes('JWT Authentication');
    const hasMongo = config.database === 'MongoDB';
    const hasPostgres = config.database === 'PostgreSQL';
    const hasMySQL = config.database === 'MySQL';
    const hasRedis = config.features.includes('Caching (Redis)');
    const hasSentry = config.features.includes('Error Tracking (Sentry)');
    return `<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
    <relativePath/>
  </parent>

  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>
  <version>1.0.0</version>
  <name>${config.projectName}</name>
  <description>${config.projectName} — Generated by Navi 🧭</description>
  <packaging>jar</packaging>

  <properties>
    <java.version>17</java.version>
  </properties>

  <dependencies>
    <!-- Core -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- Database -->
    ${hasMongo ? `<dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-mongodb</artifactId>
    </dependency>` : ''}
    ${hasPostgres || hasMySQL ? `<dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>` : ''}
    ${hasPostgres ? `<dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>` : ''}
    ${hasMySQL ? `<dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>` : ''}

    <!-- Auth / Security -->
    ${hasAuth ? `<dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>0.12.3</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>0.12.3</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>0.12.3</version>
      <scope>runtime</scope>
    </dependency>` : ''}

    <!-- Redis Cache -->
    ${hasRedis ? `<dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>` : ''}

    <!-- Sentry -->
    ${hasSentry ? `<dependency>
      <groupId>io.sentry</groupId>
      <artifactId>sentry-spring-boot-starter-jakarta</artifactId>
      <version>7.3.0</version>
    </dependency>` : ''}

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
`;
}
// ─── Application.java ─────────────────────────────────────────────────────────
function getSpringBootApplicationJava(config) {
    const pkg = getPackageName(config);
    const className = config.projectName.replace(/[\s-]/g, '') + 'Application';
    return `package ${pkg};

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * ${config.projectName} — Generated by Navi 🧭
 */
@SpringBootApplication
public class ${className} {
    public static void main(String[] args) {
        SpringApplication.run(${className}.class, args);
    }
}
`;
}
// ─── application.properties ───────────────────────────────────────────────────
function getSpringBootAppProperties(config) {
    const artifactId = getArtifactId(config);
    const hasMongo = config.database === 'MongoDB';
    const hasPostgres = config.database === 'PostgreSQL';
    const hasMySQL = config.database === 'MySQL';
    const hasRedis = config.features.includes('Caching (Redis)');
    const hasSentry = config.features.includes('Error Tracking (Sentry)');
    return `# ${config.projectName} — application.properties
# Generated by Navi 🧭

spring.application.name=${artifactId}
server.port=8080

# Database
${hasMongo ? `spring.data.mongodb.uri=mongodb://localhost:27017/${artifactId}
spring.data.mongodb.database=${artifactId}` : ''}
${hasPostgres ? `spring.datasource.url=jdbc:postgresql://localhost:5432/${artifactId}
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false` : ''}
${hasMySQL ? `spring.datasource.url=jdbc:mysql://localhost:3306/${artifactId}
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update` : ''}

# Redis
${hasRedis ? 'spring.data.redis.host=localhost\nspring.data.redis.port=6379' : ''}

# JWT (add your own secret)
jwt.secret=navi_super_secret_key_change_in_production
jwt.expiration=86400000

# Sentry
${hasSentry ? 'sentry.dsn=\nsentry.traces-sample-rate=1.0' : ''}

# Logging
logging.level.root=INFO
logging.level.${groupId}=DEBUG
`;
}
// ─── HealthController.java ────────────────────────────────────────────────────
function getSpringBootHealthController(config) {
    const pkg = getPackageName(config);
    return `package ${pkg}.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Map;

/**
 * Health check controller — Generated by Navi 🧭
 */
@RestController
@RequestMapping("/health")
public class HealthController {

    @GetMapping
    public ResponseEntity<Map<String, String>> health() {
        return ResponseEntity.ok(Map.of(
            "status", "ok",
            "message", "🧭 Navi Spring Boot is running!",
            "project", "${config.projectName}"
        ));
    }
}
`;
}
// ─── Dockerfile ───────────────────────────────────────────────────────────────
function getSpringBootDockerfile() {
    return `# Build stage
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B
COPY src ./src
RUN mvn package -DskipTests

# Run stage
FROM eclipse-temurin:17-jre-jammy
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
`;
}
// ─── .gitignore for Java ──────────────────────────────────────────────────────
function getJavaGitignore() {
    return `target/
*.class
*.jar
*.war
*.ear
.mvn/
!**/src/main/**/target/
!**/src/test/**/target/
.idea/
*.iws
*.iml
*.ipr
.vscode/
*.DS_Store
.env
application-local.properties
`;
}
//# sourceMappingURL=java.js.map