"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFrontendDeps = getFrontendDeps;
exports.getReactAppTemplate = getReactAppTemplate;
exports.getIndexHtmlTemplate = getIndexHtmlTemplate;
exports.getTailwindConfig = getTailwindConfig;
exports.getViteConfig = getViteConfig;
function getFrontendDeps(config) {
    const deps = [];
    const { frontendFramework, cssFramework, uiLibrary } = config;
    // Framework deps
    const frameworkDeps = {
        'React.js': ['react', 'react-dom'],
        'Next.js': ['next', 'react', 'react-dom'],
        'Vue.js': ['vue'],
        'Nuxt.js': ['nuxt'],
        'Angular': ['@angular/core', '@angular/common', '@angular/router'],
        'Svelte': ['svelte'],
        'SvelteKit': ['@sveltejs/kit', 'svelte'],
        'Solid.js': ['solid-js'],
        'Astro': ['astro'],
        'Remix': ['@remix-run/node', '@remix-run/react', 'react', 'react-dom'],
        'Gatsby': ['gatsby', 'react', 'react-dom'],
    };
    if (frameworkDeps[frontendFramework]) {
        deps.push(...frameworkDeps[frontendFramework]);
    }
    // CSS deps
    const cssDeps = {
        'Tailwind CSS': ['tailwindcss', 'postcss', 'autoprefixer'],
        'Bootstrap 5': ['bootstrap'],
        'Bulma': ['bulma'],
        'Materialize CSS': ['materialize-css'],
        'SASS/SCSS only': ['sass'],
    };
    if (cssDeps[cssFramework])
        deps.push(...cssDeps[cssFramework]);
    // UI Library deps
    const uiDeps = {
        'ShadCN UI': ['@radix-ui/react-dialog', 'class-variance-authority', 'clsx', 'tailwind-merge'],
        'Material UI (MUI)': ['@mui/material', '@emotion/react', '@emotion/styled'],
        'Ant Design': ['antd'],
        'Chakra UI': ['@chakra-ui/react', '@emotion/react', '@emotion/styled', 'framer-motion'],
        'Mantine': ['@mantine/core', '@mantine/hooks'],
        'DaisyUI': ['daisyui'],
        'Radix UI': ['@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu'],
        'Headless UI': ['@headlessui/react'],
        'PrimeReact': ['primereact', 'primeicons'],
        'Flowbite': ['flowbite', 'flowbite-react'],
    };
    if (uiDeps[uiLibrary])
        deps.push(...uiDeps[uiLibrary]);
    return [...new Set(deps)];
}
function getReactAppTemplate(config) {
    const { projectName, cssFramework, uiLibrary } = config;
    const hasTailwind = cssFramework === 'Tailwind CSS';
    return `import React from 'react';
${hasTailwind ? "import './index.css';" : ''}

function App() {
  return (
    <div${hasTailwind ? ' className="min-h-screen bg-gray-50 flex flex-col items-center justify-center"' : ''}>
      <h1${hasTailwind ? ' className="text-4xl font-bold text-gray-800"' : ''}>
        Welcome to ${projectName} 🧭
      </h1>
      <p${hasTailwind ? ' className="text-gray-500 mt-2"' : ''}>
        Generated by Navi - Your project is ready!
      </p>
    </div>
  );
}

export default App;
`;
}
function getIndexHtmlTemplate(config) {
    return `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>${config.projectName}</title>
  ${config.cssFramework === 'Bootstrap 5' ? '<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">' : ''}
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>
`;
}
function getTailwindConfig() {
    return `/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
`;
}
function getViteConfig(config) {
    const isReact = ['React.js', 'Next.js'].includes(config.frontendFramework);
    return `import { defineConfig } from 'vite'
${isReact ? "import react from '@vitejs/plugin-react'" : ''}

export default defineConfig({
  plugins: [${isReact ? 'react()' : ''}],
})
`;
}
//# sourceMappingURL=frontend.js.map